# Баг-репорты — JSONPlaceholder API

## BUG-01: DELETE /posts/{id} не удаляет ресурс физически

| Поле | Описание |
|---|---|
| Шаги воспроизведения | 1. DELETE /posts/1 → получен 200 OK. 2. GET /posts/1 → пост всё ещё возвращается с полным телом. |
| Ожидаемый результат | После успешного удаления повторный GET должен вернуть 404 |
| Фактический результат | Повторный GET возвращает 200 OK и исходные данные поста |
| Приоритет / severity | Low (ожидаемое поведение для mock-API без реальной БД, но задокументировано как отклонение от REST-конвенций) |

## BUG-02: POST /posts с пустым телом {} не возвращает ошибку валидации

| Поле | Описание |
|---|---|
| Шаги воспроизведения | 1. POST /posts с телом {} и Content-Type: application/json. 2. Проверить код ответа и тело. |
| Ожидаемый результат | 400 Bad Request с описанием отсутствующих обязательных полей |
| Фактический результат | API возвращает 201 Created с автосгенерированным id, игнорируя отсутствие полей |
| Приоритет / severity | Medium — отсутствие валидации на входных данных |

---

_Добавляй сюда свои реальные баг-репорты по этому же шаблону, если найдёшь расхождения при прогоне тестов._
